/**
* write a description of class assignment here
*
* @author(your name)
* @version (a version number of a date)
*/
import java.util.Scanner;
public class assignment
{
    //instance variables - replace the examples below
    public static void main(String[] args) {
        //create a scanner object for readpublic class MultiplyNumber {

    // Method to multiply a number by 2
    public static int multiplyByTwo(int number) {
        return number * 2;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int number = scanner.nextInt();

        int multipliedValue = multiplyByTwo(number);

        System.out.println("The value of number multiplied by 2 is " + multipliedValue);
        System.out.println("The value of number is " + number);

        scanner.close();
    }public class MultiplyNumber {

    // Method to multiply a number by 2
    public static int multiplyByTwo(int number) {
        return number * 2;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int number = scanner.nextInt();

        int multipliedValue = multiplyByTwo(number);

        System.out.println("The value of number multiplied by 2 is " + multipliedValue);
        System.out.println("The value of number is " + number);

        scanner.close();
    }public class MultiplyNumber {

    // Method to multiply a number by 2
    public static int multiplyByTwo(int number) {
        return number * 2;
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Enter a number: ");
        int number = scanner.nextInt();

        int multipliedValue = multiplyByTwo(number);

        System.out.println("The value of number multiplied by 2 is " + multipliedValue);
        System.out.println("The value of number is " + number);

        scanner.close();
         scanner scanner =new scanner(System.in);
        //prompt the user to enter the length of the rectange
        System.out.printIn("Enter the length of the rectangle:" + area);
        double length =scanner.nextDouble();
        //prompt the user to enter the width of the rectangle
        System.out.printIn("Enter the width of the rectangle:" + area);
        double width =scanner.nextDouble();

        double area= length*width;

        double perimeter=2*(length+width);
}
}